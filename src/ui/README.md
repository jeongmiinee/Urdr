# UI

This folder owns reusable controls, screen composition helpers, theme tokens, and feature styles.
UI components may compose domain services, localization, formatting, calendar, and graphics through their public entry points.

