# World Archive v1.0 검증 보고서

## 통과

- TypeScript 프로젝트 검사: `npm run typecheck`
- 통합 스모크: `npm run smoke`
- 지도 생성 Worker 스모크: `npm run worker:smoke`
- 프로덕션 번들: `npm run build`
- Windows 포터블 콘솔 실행기: `--smoke-test`

## 핵심 회귀 검사

- 해안 육지 2셀 안에 숲·열대림이 없음
- 도시 주변 숲 밀도가 생성 전보다 증가하지 않고 실제로 감소함
- 타임라인 드래그 중 전역 프로젝트 갱신 경로가 없음
- 라이트 모드 카테고리 문서 도구와 4초 알림 타이머가 존재함
- 지형 벡터 스키마 4, 중복 정점·장선 거부, 저장 데이터 자동 재생성
- 시계·반시계 방향 폴리곤 내부 판정과 3단 홀 중첩 분류

## 측정 결과

- 기본 미리보기: 약 411ms, 등고선 1,499개
- Worker 응답: 약 758ms, ready → accepted → result
- 데모: 512×256 계산 격자, 2048×1024 렌더 목표
- 최대 굴곡: 도로 31.65도, 등고선 33.04도, 강 15.11도

소스 Electron 스모크는 로컬 `node_modules`에 Electron 바이너리가 없고 네트워크가 제한되어 실행하지 못했습니다. 동일 배포 경로를 사용하는 포터블 실행기의 스모크는 통과했습니다.
