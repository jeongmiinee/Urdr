# World Archive v0.99x 변경 내역

## 지형 벡터 렌더링
- 모든 지형과 수역을 동일 셀 연결 영역별 벡터 멀티폴리곤으로 변환합니다.
- 내부 환경 계산은 래스터 격자를 유지하고, 화면에는 곡선 벡터 경계를 덮어 그리는 하이브리드 구조를 적용했습니다.
- 호수·해안선은 저·중·고주파 노이즈와 도메인 워핑을 강화해 사각 타일 경계를 줄였습니다.
- 확대 단계와 데이터 크기를 고려해 벡터화 LOD를 최대 512로 제한했습니다.

## 지형 패치 규모
- 실제 월드 km 면적이 작을수록 동일 지형이 더 크고 연속적인 덩어리로 형성됩니다.
- 숲 군집 노이즈, 지형 다수결 정리, 최소 패치 크기를 실제 면적에 연동했습니다.

## 농경지 생성
- 자연 지형 생성 단계에서 농경지를 제거했습니다.
- 수도·도시·마을 배치 이후 평원·초원만 농경지 후보가 됩니다.
- 강 본류·지류·담수 접근성, 도시 거리, 범람 완충거리, 경사, 수분, 기후를 종합해 농경지를 생성합니다.
- 강둑 바로 옆의 상습 범람대는 억제하고 약간 떨어진 충적평야·하안단구에 군집되도록 했습니다.
- 농경지 벡터 영역은 도시 배치 후 다시 생성됩니다.

## 지도 생성기 UI
- 시드 아래에 접이식 `고급 설정` 영역을 추가했습니다.
- 렌더 해상도 가로축, 분석·계산 격자 가로축, worldWidth/Height 배율을 한 줄 목록형 선택으로 통합했습니다.
- 세 설정의 부연 설명과 기존 배율 슬라이더를 제거했습니다.
- 접기 상태를 로컬 UI 설정으로 저장합니다.
- 별도 지도 중심 위도 항목을 제거하고 기상·기후 영역의 기준 위도를 `지도 중심 위도` 하나로 통합했습니다.

## 데모
- 지형·수역 벡터 영역, 도시·수계 기반 농경지, 강화된 해안 노이즈를 적용해 데모를 다시 생성했습니다.
- 국가 영토는 기존 데이터를 재사용하지 않고 도시 발원 상승·완전 침식·재팽창으로 다시 생성했습니다.
