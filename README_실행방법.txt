World Archive v1.0 실행 방법

1. ZIP 파일을 완전히 압축 해제합니다.
2. `World Archive.exe`를 실행합니다.
3. Windows 보안 안내가 표시되면 파일 위치와 배포 출처를 확인한 뒤 실행 여부를 선택합니다.

실행이 되지 않을 때:
- `안전 모드로 실행.cmd`를 사용합니다.
- 오류 확인이 필요하면 `World Archive - Console.exe`를 실행합니다.
- EXE 파일과 `web` 폴더를 같은 위치에 둡니다.
